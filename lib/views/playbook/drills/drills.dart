import 'package:flutter/material.dart';
import 'package:reps_trainer_app/utils/fonts.dart';

class DrillsWidget extends StatelessWidget {
  const DrillsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Drills content goes here', style: textStyleNormal,),
    );
  }
}